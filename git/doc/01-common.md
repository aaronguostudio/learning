# Checking if ssh to git is working on local
- ssh -T git@github.com

# Git commands
- git push --delete <remote_name> <branch_name>
- git branch -d <branch_name>

# git-flow
- git branching framework
